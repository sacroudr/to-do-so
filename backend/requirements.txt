# Dependances d'execution (runtime).
# Alternative a `pip install -e .` a partir de pyproject.toml.
# Pour les outils de dev (pytest, ruff, black, mypy), voir pyproject [dev].
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
# [crypto] = dependance `cryptography`, REQUISE pour verifier les tokens ES256/RS256
# (JWT Signing Keys Supabase via JWKS). Sans cet extra, un build propre casserait
# l'auth en production (les access_tokens utilisateurs sont signes en ES256).
pyjwt[crypto]>=2.9
supabase>=2.9
# Parsing des requetes multipart/form-data (upload de pieces jointes PDF, §5).
# FastAPI en a besoin des qu'une route declare un UploadFile / File.
python-multipart>=0.0.9
